# markdown-utils

Utilities for markdown conversion, built on [Sätteri](https://www.npmjs.com/package/satteri).

## Development Notes

`types/` is generated from the JSDoc annotations and committed. CI fails if it is stale, so run `npm run build:types` after changing any exported signature.
